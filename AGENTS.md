# AGENTS.md

## Cursor Cloud specific instructions

This repository ("WhooInject") is currently an empty/freshly initialized project with only a `README.md` file. No source code, dependencies, build system, or application logic exists yet.

### Current state
- Single file: `README.md`
- No package manager configured
- No programming language or framework chosen
- No services to run, test, or build

### For future agents
- Once code is added, update this file with relevant build/run/test instructions.
- Check for `package.json`, `requirements.txt`, `go.mod`, `Cargo.toml`, or similar to determine the project's language and package manager.
- Update the VM environment script accordingly when dependencies are introduced.
